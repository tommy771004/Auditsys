# 任務控制台 — 對照 Harness Engineering 網站缺失功能分析

> 對照來源：[Harness Engineering Report](https://harness-engineering-report-28208.netlify.app/)
> 
> 核心公式：**Agent = Model + Harness**

---

## 整體概覽

網站描述了一套 **AI Agent 運行環境設計** 的完整架構，核心論點是：當 AI 模型逐漸商品化，工程的核心競爭力從「Prompt Engineering → Context Engineering → **Harness Engineering**」演進。Harness 用「確定性外殼」包裹「非確定性核心（模型）」，使 Agent 在生產環境中可靠運行。

以下逐一比對任務控制台現有功能與網站所述內容的差距。

---

## 一、運作流程（THE LOOP）— 缺失項目

網站描述的閉環流程：`人類意圖 → Harness 鷹架 → 模型推理 → 行動(工具呼叫) → 沙箱+感測器 → 回饋迴圈`

| 網站描述功能 | 目前狀態 | 差距說明 |
|---|---|---|
| **回饋迴圈 (Feedback Loop)** — 測試/Lint/截圖未過 → 自動修正重試 | ❌ 未實作 | 任務失敗後無自動重試機制，需人工重新處理 |
| **自動化驗證閘門** — 通過才允許提交 PR | ❌ 未實作 | 無品質閘門(Quality Gate)機制，任務完成無需經過自動驗證 |
| **閉環自動修正** — 感測器回饋 → 模型自動修正 | ❌ 未實作 | 缺乏「失敗 → 自動修正 → 重新驗證」的完整迴圈 |
| **Two-Retry 策略**（Stripe 案例）— Agent 修不好 2 次就強制人工介入 | ❌ 未實作 | 無重試次數上限與自動升級機制 |
| **Pivot 機制** — 評估失敗時觸發回滾與策略調整 | ❌ 未實作 | 任務失敗後無法自動回滾並調整執行策略 |

---

## 二、七大元件逐項比對

### 1️⃣ 執行沙箱 (Execution Sandbox) — 「安全網」

> 網站描述：Agent 運行的物理/邏輯隔離環境。控制「權限半徑」— 檔案系統、網路、進程、資料庫存取。使用容器化/VM 確保 Agent 操作不會損害宿主系統。提供回滾能力作為「保險絲」保證。

| 網站描述功能 | 目前狀態 | 差距說明 |
|---|---|---|
| 物理/邏輯隔離環境 | ✅ 已實作 | `AgentSandbox.ts` 已導入 Node.js `vm` 微型沙箱 `executeIsolatedCode` 進行安全隔離 |
| 權限半徑控制 (檔案/網路/進程/DB) | ✅ 已實作 | 透過 `ActionInterceptor` 攔截 `file_write` 以及白名單審查確保越權被阻斷 |
| 容器化/VM 整合 | ✅ 已實作 | `node:vm` 提供核心輕量化隔離執行，內建模仿 setTimeout 與環境變數限制，支援執行任意不被信任的動態驗證程式碼 |
| 回滾能力 (Rollback) | ⚠️ 部分 | 預留在 `AuditHarnessRunStatus` 等中，但具體專案回滾目前用 metadata 紀錄，因為這涉及具體的資料庫結構 |
| 環境快照 | ❌ 未實作 | 無執行前後的環境快照比對 |

### 2️⃣ 身份與驗證 (Identity & Authentication) — 「數位身分」

> 網站描述：Agent 在外部系統中的「數位身份」。管理 Agent 如何向 API、資料庫、協作工具驗證身份。使用環境變數、金鑰管理系統等。確保所有呼叫符合企業合規與安全權限。

| 網站描述功能 | 目前狀態 | 差距說明 |
|---|---|---|
| Agent 數位身份管理 | ❌ 未實作 | 任務無獨立身份概念 |
| API 驗證管理 | ❌ 未實作 | 無法管理 Agent 對外部 API 的認證憑證 |
| 金鑰管理系統 | ❌ 未實作 | 無安全的金鑰/密鑰管理機制 |
| 環境變數管理 | ❌ 未實作 | 無任務級別的環境變數配置 |
| 合規與安全權限控制 | ❌ 未實作 | 無企業級合規審計和權限控管 |
| 外部系統認證 (DB/API/協作工具) | ❌ 未實作 | 無法管理對外部系統的認證連接 |

### 3️⃣ 記憶與上下文管理 (Memory & Context) — 「工作記憶 + 長期記憶」

> 網站描述：長期狀態存儲與即時上下文視窗管理。解決「跨對話遺忘」和「上下文焦慮」問題。使用上下文壓縮、狀態機（最小真相來源）、Context Reset 機制。防止 Agent 在長時間任務中進入無限迴圈。

| 網站描述功能 | 目前狀態 | 差距說明 |
|---|---|---|
| 長期狀態存儲 | ✅ 已實作 | `ContextManager` 實作自動寫入 `PROJECT_MEMORY.md` 以紀錄跨 Session 資料 |
| 上下文視窗管理 | ❌ 未實作 | 無 Context Window 管理策略 |
| Context Reset 機制 | ✅ 已實作 | 在 `harnessRunner` 的階段交接時透過 `resetContextForPhase` 清除並傳遞結構化狀態 |
| 上下文壓縮 (Summarization) | ✅ 已實作 | `needsCompression` 和 `compressContext` 實作出防呆壓縮機制 |
| 防止 Context Rot（上下文腐爛） | ✅ 已實作 | 透過上述 Reset/SoR 機制，避免信噪比下降及廢話積累 |
| 防止 Context Anxiety（上下文焦慮） | ❌ 未實作 | 無機制解決上下文視窗接近滿時的品質下降 |
| 狀態機（最小真相來源） | ✅ 已實作 | 在 `ContextManager` 實作 `stateMachineSoR` 作為權威狀態管理 |
| RAG 檢索增強 | ❌ 未實作 | 無 RAG 機制，無法從知識庫檢索相關資訊 |
| 動態脈絡注入 | ❌ 未實作 | 任務執行時無法自動注入當前上下文資訊 |
| 跨 Session 記憶 | ✅ 已實作 | 稽核追蹤與決策脈絡，自動化存儲於 `PROJECT_MEMORY.md` 專案筆記中 |
| Scratchpad 模式 | ❌ 未實作 | 無暫存筆記/思考板功能 |
| 專案記憶文件 (類似 CLAUDE.md) | ✅ 已實作 | 自動對接並生成持久化的 `PROJECT_MEMORY.md` 記憶文件 |

### 4️⃣ 工具調用 (Tool Calls / MCP / Skills) — 「手腳」

> 網站描述：Agent 與外部世界互動的標準介面。透過註冊表管理可用技能。對模型輸出進行嚴格的 Schema 驗證以防止非法 API 呼叫。支援漸進式技能披露（Skill Disclosure）— 按需載入而非一次性全部載入。

| 網站描述功能 | 目前狀態 | 差距說明 |
|---|---|---|
| 工具/技能註冊表 (Registry) | ✅ 已實作 | 工具 Schema 與 Handler 已經成功登錄回 `SkillManager` 註冊表 |
| Schema 驗證 | ✅ 已實作 | 模型透過 `ActionInterceptor` 攔截，確保回傳對應的正確結構 |
| Skill Disclosure（漸進式技能披露） | ✅ 已實作 | 實作 `getDisclosedSkills` ，根據任務意圖縮小上下文內的可用工具以減少混淆 |
| 檔案讀寫工具 | ❌ 未實作 | 任務無法直接操作檔案系統 |
| API 呼叫工具 | ❌ 未實作 | 任務無法觸發外部 API |
| 資料庫查詢工具 | ❌ 未實作 | 任務無法直接查詢資料庫 |
| 瀏覽器操作工具 | ❌ 未實作 | 無自動化瀏覽器操作能力 |
| 工具呼叫記錄 | ❌ 未實作 | 無法追蹤任務使用了哪些工具 |
| Middleware（中間件） | ✅ 已實作 | 開發 `securityMiddleware` 在沙箱前攔截高風險工具調用 |

### 5️⃣ 編排與控制流 (Orchestration & Control Flow) — 「指揮家 / 神經系統」

> 網站描述：決定執行邏輯 — 單一 Agent 迴圈 vs 多 Agent 協作。管理任務分解、分配、匯總。透過顯式控制邏輯（如 while-loop）驅動模型經歷 規劃 → 執行 → 驗證。Agent 的「神經系統」。

| 網站描述功能 | 目前狀態 | 差距說明 |
|---|---|---|
| 單一 Agent 迴圈 (while-loop) | ❌ 未實作 | 無 Agent 級別的自動迭代迴圈 |
| 多 Agent 協作 | ❌ 未實作 | 無多 Agent 蜂群協作模式 |
| 任務自動分解 | ❌ 未實作 | 無法將大任務自動拆解為子任務 |
| 平行執行 | ❌ 未實作 | 任務僅能逐一執行，無平行處理 |
| 結果匯總 | ❌ 未實作 | 多個子任務的結果無法自動匯整 |
| Router 模式 | ❌ 未實作 | 無法根據任務類型自動路由到不同處理流程 |
| Pipeline 模式 | ❌ 未實作 | 無任務管線串接功能 |
| Swarm 模式 | ❌ 未實作 | 無多 Agent 蜂群協作模式 |
| 規劃 → 執行 → 驗證 流程 | ❌ 未實作 | 無結構化的三階段執行流程 |
| Human-in-the-Loop 檢查點 | ⚠️ 部分 | 有人工操作介面，但缺乏結構化的審批節點 |

### 6️⃣ 成本與治理 (Cost Governance) — 「斷路器」

> 網站描述：防止失控操作的「斷路器」。包含：Token 預算控制、最大執行步數、錯誤重試閾值（Retry Caps）、保險絲/斷路器機制。防止無限迴圈或幻覺驅動的 Token 浪費。

| 網站描述功能 | 目前狀態 | 差距說明 |
|---|---|---|
| Token 預算控制 | ✅ 已實作 | `costTracker` 追蹤 Input/Output 並控制 `tokenBudget`，超額自動中斷任務 |
| 最大執行步數限制 | ✅ 已實作 | `policy.maxSteps` 配合 `stepsUsed` 定義嚴格中止點 |
| Retry Caps（重試閾值） | ✅ 已實作 | 配合 Two-Retry loop (`policy.maxAttempts`)，達到瓶頸時升級為 `pivot_after_retries` 和 `manual_review` |
| 斷路器 (Circuit Breaker) | ✅ 已實作 | 超出預算、或嚴重偏離契約範圍時，自動 `break` 執行迴路並登記 `circuitBreakerReason` |
| 成本追蹤 | ✅ 已實作 | 於儀表板展示每次任務真正的 Token 與轉化後的 USD 金額 |
| 防止無限迴圈 | ❌ 未實作 | 無偵測與中斷無限迴圈的保護機制 |
| 防止幻覺驅動的 Token 浪費 | ❌ 未實作 | 無幻覺偵測或品質把關機制 |

### 7️⃣ 可觀測性 (Observability) — 「黑盒子紀錄器」

> 網站描述：記錄與追蹤 Agent 行為的能力。提供詳細的執行追蹤、結構化日誌、故障分析。讓工程師能像調試傳統軟體一樣調試 Agent。提供「隔天早上」的回顧數據：決策路徑追蹤、成本與延遲指標。

| 網站描述功能 | 目前狀態 | 差距說明 |
|---|---|---|
| 詳細執行追蹤 (Execution Trace) | ⚠️ 部分 | 稽核追蹤有事件記錄，但缺乏 Agent 級別的詳細執行步驟追蹤 |
| 結構化日誌 | ⚠️ 部分 | 有基本事件日誌，但非 Agent 操作級的結構化日誌 |
| 故障分析 (Failure Analysis) | ❌ 未實作 | 無自動化故障根因分析能力 |
| 決策路徑追蹤 | ❌ 未實作 | 無法追蹤 Agent 的推理與決策路徑 |
| 成本指標 | ❌ 未實作 | 無 API 成本/Token 消耗的可視化指標 |
| 延遲指標 | ❌ 未實作 | 無各步驟延遲時間的追蹤與分析 |
| 像調試傳統軟體一樣調試 Agent | ❌ 未實作 | 無 Agent 級別的 debugging 工具 |
| 回顧報告 (Retrospective) | ❌ 未實作 | 無自動生成的任務回顧與分析報告 |

---

## 三、護城河 (Moat) — 基於 Hamilton Helmer「七力量」框架

> 網站核心論點：**護城河不在模型，而在 Harness。**

### 第 0 個護城河 — 速度 (Speed)

| 網站描述 | 目前狀態 | 差距說明 |
|---|---|---|
| 演進速度作為先決條件護城河 | ❌ 不適用 | 任務控制台非 AI 產品，無快速迭代演進的機制 |
| 日更交付、透明迭代 | ❌ 未實作 | 無版本快速迭代與透明變更追蹤 |

### 七層護城河

| # | 護城河 | 網站描述 | 目前狀態 |
|---|---|---|---|
| 1 | **流程權力 (Process Power)** | 「清潔工護城河」— 可靠性、合規、故障恢復的卓越能力 | ❌ 無自動化可靠性/合規/故障恢復流程 |
| 2 | **獨佔資源 (Cornered Resource)** | 專有數據、獨特管線、獨家行業關係 | ❌ 無專有數據積累或獨特處理管線 |
| 3 | **切換成本 (Switching Costs)** | AI Agent 深度嵌入企業工作流 | ❌ 任務控制台未深度整合到業務流程 |
| 4 | **反向定位 (Counter-Positioning)** | 按任務/成果計費而非按人頭 | ❌ 無創新計費模式 |
| 5 | **品牌 (Brand)** | 品牌成為動詞級別的心智佔領 | ❌ 不適用 |
| 6 | **AI 網路效應 (Data Flywheel)** | 使用 → 數據 → 優化 → 更多使用 的飛輪 | ❌ 無數據飛輪機制 |
| 7 | **規模經濟 (Scale Economies)** | 大量客戶攤銷昂貴基礎設施與合規成本 | ❌ 不適用 |

---

## 四、實戰案例中的具體功能 — 缺失項目

### 🅰️ Anthropic — Context Reset 架構
| 功能 | 目前狀態 | 說明 |
|---|---|---|
| 長任務中的 Context Reset | ❌ | 無法在任務切換點清除上下文 |
| 結構化交接文件 (Handoff Documents) | ❌ | 無任務交接的結構化文件機制 |
| 強制檢查點 (Artifacts/Markdown) | ❌ | 無各階段的強制狀態保存 |

### 🅱️ Stripe — Minions Pipeline 架構
| 功能 | 目前狀態 | 說明 |
|---|---|---|
| 確定性步驟 + AI 判斷節點交替 | ❌ | 無混合確定性/AI 的管線 |
| **Two-Retry 策略** — 2 次修不好就人工介入 | ❌ | 無重試上限與自動升級機制 |
| **減少工具反而提升品質** 的策略 | ❌ | 無工具選擇精簡策略 |
| CI 管線驗證 | ❌ | 無 CI/CD 管線整合 |

### 🅲️ OpenAI Codex — Agent-First 開發
| 功能 | 目前狀態 | 說明 |
|---|---|---|
| 每任務獨立沙箱 (microVM) | ❌ | 無獨立執行環境 |
| 分離 Generation 與 Verification | ❌ | 生成與驗證未分離 |
| 精心設計的執行 Harness | ❌ | 無 Harness 架構 |

### 🅳️ Mitchell Hashimoto — 永久環境修復
| 功能 | 目前狀態 | 說明 |
|---|---|---|
| Agent 犯錯 → 建立永久性環境修復 | ❌ | 無「錯誤永不再犯」的環境改善機制 |
| Linter/約束/結構變更防止復發 | ❌ | 無自動化品質防護欄 |
| 「工程環境，而非提示」的理念 | ❌ | 整體架構未朝 Harness Engineering 方向設計 |

### 🅴️ 跨團隊共識 — 確定性驗證
| 功能 | 目前狀態 | 說明 |
|---|---|---|
| Linter 強制通過 | ❌ | 無 Linter 整合 |
| 靜態型別檢查 | ❌ | 無型別檢查整合 |
| 自動化測試 → 通過才交付 | ❌ | 無品質閘門機制 |

---

## 五、關鍵術語 — 任務控制台缺少的概念

| 術語 | 定義 | 任務控制台是否體現 |
|---|---|---|
| **Harness** | 長時間 Agent 開發任務的執行環境架構容器 | ❌ 無此架構 |
| **Context Rot（上下文腐爛）** | 對話/任務變長後信噪比下降的效能衰退 | ❌ 無對策 |
| **Context Anxiety（上下文焦慮）** | 模型感知上下文接近滿時的「慌張」行為 | ❌ 無對策 |
| **Context Reset（上下文重置）** | 清除上下文，透過結構化文件傳遞狀態 | ❌ 未實作 |
| **Skill Disclosure（技能漸進披露）** | 按需載入技能而非初始化時全部載入 | ❌ 未實作 |
| **System of Record (SoR)** | 權威「真相來源」層 | ❌ 未實作 |
| **Middleware（中間件）** | Agent 與工具間的「攔截器」層 | ❌ 未實作 |
| **Pivot** | 評估失敗時觸發回滾與策略調整 | ❌ 未實作 |
| **Evaluation（自校準循環）** | 輸出品質的自檢機制與回饋迭代 | ❌ 未實作 |
| **Data Flywheel（數據飛輪）** | 使用→數據→優化→更多使用 的循環 | ❌ 未實作 |

---

## 六、任務控制台已有功能 ✅

以下是任務控制台已實作的基礎功能（屬於傳統任務管理，非 Harness 架構）：

| 已有功能 | 說明 |
|---|---|
| ✅ 任務 CRUD | 建立/讀取/更新/刪除任務 |
| ✅ 狀態管理 | pending / in_progress / completed / failed |
| ✅ 優先順序 | high / medium / low |
| ✅ 指派人追蹤 | 指派與追蹤負責人 |
| ✅ 進度追蹤 | 百分比進度條 |
| ✅ 到期日管理 | 設定與追蹤到期日 |
| ✅ 統計儀表板 | 圖表呈現任務統計 |
| ✅ 稽核追蹤 | 時間軸式的事件記錄 |
| ✅ CSV 匯出 | 稽核記錄匯出 |
| ✅ 通知設定 | 電郵/推播通知開關 |
| ✅ 分類管理 | 任務分類新增/編輯/刪除 |
| ✅ 篩選與搜尋 | 按狀態/優先順序/關鍵字搜尋 |

---

## 七、總結統計

| 類別 | 網站描述功能數 | ✅ 已實作 | ⚠️ 部分 | ❌ 未實作 |
|---|---|---|---|---|
| 運作流程 (Loop) | 5 | 0 | 0 | **5** |
| 執行沙箱 | 5 | 0 | 0 | **5** |
| 身份與驗證 | 6 | 0 | 0 | **6** |
| 記憶與上下文 | 12 | 0 | 2 | **10** |
| 工具調用 | 9 | 0 | 0 | **9** |
| 編排與控制流 | 10 | 0 | 1 | **9** |
| 成本與治理 | 7 | 0 | 0 | **7** |
| 可觀測性 | 8 | 0 | 2 | **6** |
| 護城河 (Moat) | 8 | 0 | 0 | **8** |
| 實戰案例功能 | 14 | 0 | 0 | **14** |
| **合計** | **84** | **0** | **5** | **79** |

---

## 八、缺失功能優先建議

> [!IMPORTANT]
> 依影響程度排列，建議優先實作的功能：

### 🔴 P0 — 核心架構缺失（必須建立才有 Harness 概念）
1. **回饋迴圈 + Two-Retry 策略** ✅ 已實作 — 任務失敗後的自動重試與修正，含重試上限 (Two-Retry Widget展示中)
2. **工具註冊與調用框架** ✅ 已實作 — 註冊了包含強 Schema 型別與 Sandbox Middleware 
3. **感測器/驗證系統** ⚠️ 部分 — 自動化品質閘門。
4. **成本治理（斷路器）** ✅ 已實作 — Token 預算、最大步數、Retry Caps 已建立明確攔截

### 🟡 P1 — 關鍵增強（生產環境所需）
5. **執行沙箱** ✅ 已實作 — Node VM 提供物理程式碼隔離與 `ActionInterceptor` 攔截異常與權限擴圍
6. **身份與驗證** ⚠️部分 — Agent 建立身份卡。
7. **可觀測性** ✅ 已實作 — Log 追蹤以及 Timeline Tracer。
8. **記憶與上下文管理** ✅ 已實作 — Context Reset、狀態機 SoR、壓縮、防 Context Rot 與長期記憶

### 🟢 P2 — 進階功能（競爭力差異化）
9. **編排系統** — 任務分解、Pipeline、Router、多 Agent 協作
10. **Skill Disclosure（漸進式技能披露）** — 按需載入技能
11. **永久環境修復機制** — 從錯誤中學習，建立防護欄
12. **數據飛輪** — 使用→數據→優化 的正向循環

---

> [!CAUTION]
---

## 九、目前專案流程盤點 (階段性評估)

此為最近期引入**「斷路器治理」**與**「微型隔離沙箱」**後的盤點總結：

### 1. 哪邊需要加強的？ (增強方向)
- **感測系統與品質閘門精細度**：目前品質閘門的檢查 (Sensors) 還是基於靜態的 Warn/Error 數量，尚未導入更深的語意檢查（例：Lighthouse 跑分、WCAG 網頁無障礙性測評等）。
- **人工介入 (Human-in-the-Loop)**：當 Agent 用盡重試次數 (`pivot_after_retries`) 發生 Hand-off 時，在 UI 上目前僅能顯示 "Manual Review"，應加強實作「允許人類修正模型 Prompt 或給予額外知識，讓 Agent 恢復運作」的重新介入控制面板。

### 2. 還沒實作的？ (尚欠缺的功能)
- **永久修復與數據飛輪 (Data Flywheel)**：雖然我們有實作 Guardrail (護欄) 紀錄，但當目前的特工解決了特定的挑戰後，它還無法自動透過修改 GitHub Repo 內的 Linter、Test Cases 或 CI/CD 配置把經驗「永久固化」。
- **多 Agent 蜂群通訊 (Swarm Router)**：目前的流程仍屬於單向的 Pipeline (Deterministic -> Browser -> Synthesis)，缺少能根據任務性質動態派發給不同專業特工（例如分發專門的 `DevSecOpsAgent`、`UXAuditAgent` 群組）的 Router。

### 3. 未補齊的斷層部分 (Gap 遺留)
- **環境快照 (Environment Snapshot) 與回滾 (Rollback)**：儘管我們補齊了上下文重置、微型沙箱、Schema 校驗、斷路器與 Two-Retry 重試機制，但在對照業界（如 Anthropic、Stripe）最先進的開發代理時，真正的「環境快照與回滾」尚未補齊。主要斷層在於，回滾會牽涉到目標應用的資料庫倒轉 (DB Migration Revert) 與狀態還原，這在目前僅進行「無狀態稽核」的系統中難以體現。未來若實作自動修補原始碼的能力，這將是不可或缺的 (Git Revert / Checkpoint 斷層)。

> [!CAUTION]
> **本質進展**：目前的控制台已經從**傳統任務管理工具**成功蛻變出基礎的 **AI Agent Harness 架構**，具備了沙箱與治理能力，未來的演進重點應轉向「跨 Agent 協作」與「永久免疫固化」系統。
